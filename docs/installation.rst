============
Installation
============

At the command line::

    $ easy_install django-genomix

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-genomix
    $ pip install django-genomix
